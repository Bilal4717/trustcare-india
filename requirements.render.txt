# Install profile for Render (Web Service) and similar Linux PaaS hosts.
# Gunicorn for Linux PaaS. FAISS is in main requirements.txt.
#
#   pip install -r requirements.render.txt
#
-r requirements.txt
gunicorn>=22.0
