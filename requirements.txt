# ── Web framework ──────────────────────────────────────────────────────────────
flask>=3.0
python-dotenv>=1.0

# ── Data processing ────────────────────────────────────────────────────────────
pandas>=2.0
openpyxl>=3.1
numpy>=1.26
# Required for FAISS vector retrieval (Vercel / any host without Mosaic)
faiss-cpu>=1.8

# ── LangChain / LangGraph core ────────────────────────────────────────────────
langchain-core>=0.3
langchain-community>=0.3
langgraph>=0.2
pydantic>=2.0

# ── Web search enrichment ─────────────────────────────────────────────────────
tavily-python>=0.5.0

# ── LLM providers (choose one or more) ───────────────────────────────────────
# Google Gemini — primary recommended path (free tier available)
langchain-google-genai>=2.0.0

# OpenAI — optional cloud alternative
langchain-openai>=0.2

# ── Databricks integration (install for Databricks-native stack) ──────────────
# Uncomment these when running against a Databricks workspace:
# databricks-sdk>=0.20.0
# databricks-sql-connector>=3.0.0
# mlflow[databricks]>=3.0.0
